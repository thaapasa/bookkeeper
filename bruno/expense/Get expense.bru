meta {
  name: Get expense
  type: http
  seq: 2
}

get {
  url: {{SERVER_URL}}/api/expense/:expenseid
  body: none
  auth: inherit
}

params:path {
  expenseid: {{EXPENSE_ID}}
}

settings {
  encodeUrl: true
  timeout: 0
}

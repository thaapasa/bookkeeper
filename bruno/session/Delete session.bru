meta {
  name: Delete session
  type: http
  seq: 6
}

delete {
  url: {{SERVER_URL}}/api/session
  body: none
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}

# Frontend: Mantine 8 UI

Read `src/client/coding-conventions.md` before writing or modifying any UI code.
It is the single source of truth for frontend coding conventions.

When touching a file with legacy patterns (Emotion styled wrappers, MUI imports, class
components, raw pixel values for spacing), rewrite it from scratch with Mantine rather
than patching the old code.

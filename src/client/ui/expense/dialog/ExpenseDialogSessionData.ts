// Re-export the type and hook from SessionStore for backward compatibility.
export type { ExpenseDialogData } from 'client/data/SessionStore';
export { useExpenseDialogData } from 'client/data/SessionStore';
